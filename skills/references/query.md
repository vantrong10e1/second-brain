# Query

Search only inside `wiki/`.

If one or more matching pages are found:

- Answer only using information from those pages.

If no matching information exists:

Return exactly:

> Kết quả này không tìm thấy hoặc tồn tại trong Bộ Não Nhân Tạo.

Stop immediately.

Do not:

- explain the search result;
- report missing pages;
- suggest creating a page;
- suggest ingesting data;
- ask follow-up questions;
- provide any additional text.